"""A minimal terminal dungeon crawler on the plain standard library.

Run it with `uv run python -m examples.tui_crawler` (add `--seed N --script FILE`
for a non-interactive playthrough). The example is the spec's extension-surface
proof: its fetch quest lives entirely in game code on the listener/flags surface —
no library changes.
"""
